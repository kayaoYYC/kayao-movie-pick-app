# agy-cli-gh-demo

A demo project demonstrating GitHub CLI setup and Python virtual environment configuration.

## Setup

1. **Python Virtual Environment**:
   To activate the virtual environment:
   ```bash
   source .venv/bin/activate
   ```

2. **GitHub CLI**:
   Authenticate with GitHub:
   ```bash
   gh auth login
   ```
